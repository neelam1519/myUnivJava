<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_youraccount"
            android:icon="@drawable/youraccount"
            android:title="Your Account" />
        <item
            android:id="@+id/nav_review"
            android:icon="@drawable/review"
            android:title="Review" />
    </group>

    <!-- Create a new group for the Logout and Settings items -->
    <group
        android:id="@+id/group_bottom"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/settings"
            android:title="Settings" />
        <item
            android:id="@+id/nav_logout"
            android:icon="@drawable/logout"
            android:title="Logout" />
    </group>
</menu>
